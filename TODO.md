# TODO

## S3 ファイル compaction（IoT センサーデータ）

**背景**

IoT センサーデータは 1メッセージ = 1 S3 ファイルで蓄積されており、1時間あたり約60ファイルが生成される。
Athena はクエリのたびにパーティション内のファイルを1件ずつ LIST/GET するため、ファイル数に比例して S3 リクエストコストが増加する。

2026-07 に Grafana の Athena クエリパーティションフィルタを月単位→日単位に修正済み（Tier2 GET の削減）。
compaction を実装すると Tier1 LIST も含めてさらに削減できる。

**実装方針**

- 場所: `car-iot-services` リポジトリの `infra/lambda_src/compaction/`
- トリガー: EventBridge Scheduler、1時間ごと
- 処理対象: 1時間前のパーティション（書き込み完了済みが確実なもの）
  - 例: 15:00 実行 → `raw/year=.../month=.../day=.../hour=14/` を処理
- 処理内容:
  1. 対象パーティション内のファイルを LIST
  2. 全ファイルを GET して JSON レコードを読み込む
  3. 改行区切り JSON（NDJSON）として1ファイルに結合し PUT
  4. 元のファイルを DELETE
- スキーマ変更なし（Athena の JSON SerDe は単一 JSON・NDJSON の混在を透過的に読める）
- 既存データにも遡って適用可能（バッチ実行で全パーティションを処理）

**実施タイミング**

パーティションフィルタ修正の効果を 2026-08 の請求で確認してから判断する。
S3 コストが $20 台以下に収まっていれば保留、まだ高ければ実装に着手する。
